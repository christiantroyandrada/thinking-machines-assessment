services:
  - type: web
    name: worksmart-api
    env: docker
    rootDir: server
    envVars:
      - key: DATABASE_URL
        value: file:/var/data/worksmart.db
    disk:
      name: worksmart-data
      mountPath: /var/data
      sizeGB: 1
